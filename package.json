{
  "name": "isidore",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "index": "bun tools/vector-index.ts",
    "index:hook": "bun tools/vector-index.ts --install-hook",
    "search": "bun tools/vector-search.ts",
    "snapshot": "bun tools/snapshot.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
